# 拓扑排序的关键路径、关键点

## 1. 问题模型

对于 DAG（有向无环图），若：

```
u -> v
```

表示 `v` 必须等 `u` 完成后才能开始。

设每个节点有耗时：

```cpp
duration[u]
```

常见问题分三层：

1. 求整个项目最短完成时间
2. 求一条关键路径
3. 求所有关键节点、slack、critical edges / critical subgraph

---

## 2. 只求总完成时间：一次正向拓扑 DP

定义：

```
earliestFinish[u]
= u 最早完成时间
```

对于所有入度为 0 的节点：

```cpp
earliestFinish[u] = duration[u];
```

对于边：

```
u -> v
```

做转移：

```cpp
earliestFinish[v] =
    max(earliestFinish[v],
        earliestFinish[u] + duration[v]);
```

最终：

```cpp
T = max(earliestFinish[u]);
```

其中 `T` 就是整个项目的最短完成时间。

这本质上是：

> Topological Sort + Longest Path DP on DAG

例如 LC 2050 — Parallel Courses III。

---

## 3. 求一条关键路径：正向拓扑 DP + parent

如果只要求恢复一条关键路径，可以在更新最长路径时同时记录前驱：

```cpp
if (earliestFinish[u] + duration[v] > earliestFinish[v]) {
    earliestFinish[v] = earliestFinish[u] + duration[v];
    parent[v] = u;
}
```

最后找到：

```cpp
end = argmax(earliestFinish);
```

然后沿：

```
end -> parent[end] -> parent[parent[end]] -> ...
```

反向恢复一条关键路径。

注意：

> 如果存在多条同样长的关键路径，单个 `parent[v]` 只能恢复其中一条。

所以现实里只求“一条关键路径”通常信息量有限。

---

## 4. 为什么现实中更关心所有关键点

实际调度中通常更关心：

- 哪些任务是 critical
- 哪些任务一延迟就会拖慢整个项目
- 每个任务有多少 slack / float
- 哪些边是 critical edge
- 是否存在多条 critical paths
- 整个 critical subgraph 是什么

因此一般需要：

```
forward topo DP
+
reverse topo DP
```

---

## 5. 正向 DP：计算 earliest

定义：

```
earliestStart[u]
earliestFinish[u]
```

有：

```
earliestFinish[u]
= earliestStart[u] + duration[u]
```

对于边：

```
u -> v
```

正向更新：

```cpp
earliestStart[v] =
    max(earliestStart[v], earliestFinish[u]);
```

最终：

```cpp
T = max(earliestFinish[u]);
```

---

## 6. 反向 DP：计算 latest

为了找 slack，需要从终点往回计算：

```
latestFinish[u]
latestStart[u]
```

对于最终终点，可初始化：

```
latestFinish[u] = T
```

对于边：

```
u -> v
```

反向更新：

```cpp
latestFinish[u] =
    min(latestFinish[u], latestStart[v]);
```

然后：

```
latestStart[u]
= latestFinish[u] - duration[u]
```

所以完整思路是：

```
forward topo
    -> earliest

reverse topo
    -> latest
```

---

## 7. Slack / Float

节点的 slack：

```
slack[u]
= latestStart[u] - earliestStart[u]
```

也等价于：

```
slack[u]
= latestFinish[u] - earliestFinish[u]
```

如果：

```
slack[u] == 0
```

那么节点 `u` 是关键节点。

含义：

> 这个任务没有时间余量，它一旦延迟，就可能导致整个项目完成时间延迟。

如果：

```
slack[u] > 0
```

说明它可以在 slack 范围内延迟，而不影响最终项目完成时间。

---

## 8. 关键节点

关键节点的本质：

> 节点位于至少一条最长依赖路径上，并且没有 slack。

也可以从 DAG 双向最长路角度理解。

定义：

```
forward[u]
= 从 source 到 u 的最长时间

backward[u]
= 从 u 到 sink 的最长时间
```

如果节点权重是 `duration[u]`，并且项目最长时间是 `T`，则：

```
forward[u] + backward[u] - duration[u] == T
```

说明 `u` 位于某条最长路径上。

这是一种非常通用的判断方法。

---

## 9. 关键边

对于边：

```
u -> v
```

一个常见判断是：

```
earliestFinish[u] == earliestStart[v]
```

并且 `u`、`v` 都位于关键结构中。

也可以用双向 DP 判断一条边是否位于某条最长路径上：

```
forward[u] + duration[v] + backward[v] - duration[v] == T
```

具体公式取决于 forward / backward 的定义是包含当前节点还是不包含当前节点，因此实现时要保持定义一致。

---

## 10. 所有关键点不一定只组成一条路径

例如：

```
      B
     / \
A --    -- D
     \ /
      C
```

如果：

```
A -> B -> D
A -> C -> D
```

两条路径长度相同，那么：

```
A, B, C, D
```

都可能是关键节点。

所以现实中真正有价值的对象通常不是“一条 critical path”，而是：

```
critical nodes
critical edges
critical-path DAG / critical subgraph
slack
```

---

## 11. 一次 topo vs 两次 topo

| 目标 | 方法 |
|---|---|
| 总完成时间 | 正向 topo + longest path DP |
| 一条关键路径 | 正向 topo + parent |
| 所有关键节点 | 正向 DP + 反向 DP |
| slack | 正向 + 反向 |
| critical edges | 正向 + 反向 |
| critical subgraph | 正向 + 反向 |

可以直接记：

```
一次 forward DP
    = 求全局最优值

forward + backward DP
    = 分析谁对这个最优值是关键的
```

---

## 12. 本质：DAG 上的 Prefix + Suffix DP

它和数组中的 prefix / suffix 思想非常类似：

```
forward topo DP
= prefix information

reverse topo DP
= suffix information
```

统一形式：

```
forward[u]
= source -> u 的最优值

backward[u]
= u -> sink 的最优值
```

然后组合：

```
forward[u] + backward[u]
```

就可以分析：

- 节点是否位于最优路径
- 节点是否关键
- 边是否关键
- 节点 / 边有多少余量
- 哪些结构会影响最终结果

---

## 13. 心智模型

```
DAG
│
├── Topological Sort
│
├── Forward DP
│   ├── earliest time
│   ├── longest path
│   └── total project duration
│
├── Parent
│   └── recover one critical path
│
└── Reverse DP
    ├── latest time
    ├── slack
    ├── critical nodes
    ├── critical edges
    └── critical subgraph
```

---

## 14. 最核心结论

> **关键路径问题，本质是 DAG 最长路径。**

> **求一条关键路径：一次正向拓扑 DP + parent 就够。**

> **求所有关键节点、slack、关键边、critical subgraph：通常需要 forward DP + backward DP。**

最值得记的一句话：

```
Forward DP 解决“最优值是多少”
Forward + Backward DP 解决“谁对这个最优值是关键的”
```
